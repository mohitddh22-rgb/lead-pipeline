{
  "name": "lead-pipeline",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Serverless real-estate lead generation + cold outreach pipeline for Vercel",
  "scripts": {
    "dev": "vercel dev",
    "deploy": "vercel deploy --prod",
    "smoke": "node scripts/smoke.mjs"
  },
  "dependencies": {
    "googleapis": "^144.0.0",
    "resend": "^3.5.0",
    "@upstash/qstash": "^2.0.0",
    "@upstash/redis": "^1.28.0"
  },
  "engines": { "node": ">=18" }
}
